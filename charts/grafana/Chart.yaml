apiVersion: v2
name: grafana
description: A Helm chart wrapper for Grafana with HA defaults, resource management, and OIDC support
type: application
version: 0.1.1
appVersion: "latest"

dependencies:
  - name: grafana
    version: "7.3.0"
    repository: "https://grafana.github.io/helm-charts"
    condition: grafana.enabled
